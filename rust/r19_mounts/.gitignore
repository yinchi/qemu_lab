# Generated by `just disk` (see the justfile and test/mkfixtures.py). Earlier stages name their programs `*.exe`,
# which the repository's root `.gitignore` covers; this stage's have no extension, so they are listed here.
/disk/bin/
/disk/root/
/disk/tests/bigpad
/disk/tests/elf-*
/disk/tests/bigimage
/disk/tests/heapuse
/disk/tests/overflow
/disk/tests/probe
/disk/tests/spin
# The persistent HOME disk (`just home-disk`): user data, made once and never rebuilt or committed.
/home.img
