//! What the Stage 18 programs share. Empty for now: the pure helpers the filters need (a glob matcher, `cut` lists, `tr`
//! sets) arrive with them and are host-tested like the kernel's pure modules.

#![no_std]
