[package]
name = "progs"
version = "0.1.0"
edition = "2024"

[dependencies]
abi = { path = "../abi" }
userlib = { path = "../userlib" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
